{
  "name": "age-pool-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "LAN-hosted app for collecting ages, collecting guesses, and showing admin results.",
  "scripts": {
    "check:firebase": "node scripts/check-firebase-setup.js",
    "start": "node server.js",
    "test": "node --test test/server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
